---
phase: 22-support-updating-a-group-of-similar-web-elements-together-sa
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/unit/test_index_expansion.py
  - tests/unit/test_workflow_models.py
  - tests/unit/test_json_loader.py
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-07, D-08, D-09, reserved, no-regression]
must_haves:
  truths:
    - "All D-01..D-09 + reserved + no-regression test methods exist and currently FAIL (RED) because the implementation is absent (Wave 1)"
    - "Test method names match VALIDATION.md exactly so the per-task verify commands resolve"
    - "Engine-level tests run without a real browser (ActionFactory.run / WaitManager mocked, real ResultCollector)"
  artifacts:
    - path: "tests/unit/test_index_expansion.py"
      provides: "TestIndexExpansion engine-level tests covering D-02a, D-03, D-03b, D-04, D-05, D-07, D-08, D-09, D-09b, no-regression"
      contains: "class TestIndexExpansion"
    - path: "tests/unit/test_workflow_models.py"
      provides: "TestIndexRange Pydantic validation tests covering D-01, D-02b, D-02c"
      contains: "class TestIndexRange"
    - path: "tests/unit/test_json_loader.py"
      provides: "TestReservedParamName reserved-name tests (load + load_raw)"
      contains: "class TestReservedParamName"
  key_links:
    - from: "tests/unit/test_index_expansion.py"
      to: "src.workflow.workflow_engine.WorkflowEngine._run_section"
      via: "engine._run_section(section, ctx) with patched ActionFactory.run"
      pattern: "_run_section"
    - from: "tests/unit/test_index_expansion.py"
      to: "src.actions.action_factory.ActionFactory.run"
      via: "patch('src.actions.action_factory.ActionFactory.run', ...) side_effect sequencing per index"
      pattern: "ActionFactory.run"
---

<objective>
Author the full RED test matrix for Phase 22 index_range loop expansion BEFORE any
implementation exists. Every behavior locked in CONTEXT.md (D-01..D-09), plus the
reserved-name and no-regression behaviors, gets a named, automated test that matches
VALIDATION.md's command table exactly.

Purpose: Establish the Nyquist validation contract (Wave 0). These tests fail now and
pass after Wave 1 (Plan 02) lands the implementation. This guarantees the implementation
is verifiable, not merely "looks done."
Output: Three test artifacts — one new file plus two new test classes appended to
existing files — covering the entire D-01..D-09 + reserved + no-regression matrix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-support-updating-a-group-of-similar-web-elements-together-sa/22-CONTEXT.md
@.planning/phases/22-support-updating-a-group-of-similar-web-elements-together-sa/22-VALIDATION.md
@.planning/phases/22-support-updating-a-group-of-similar-web-elements-together-sa/22-PATTERNS.md

<interfaces>
<!-- Contracts the tests assert against. Extracted from codebase — do not re-explore. -->

ElementDefinition (src/models/workflow_models.py) — Wave 1 adds:
  index_range: Optional[List[int]] = None    # None = single element, no loop
  validate_index_range  @model_validator(mode="after") — raises ValueError (Pydantic wraps as ValidationError) on len != 2 or start > end

WorkflowEngine (src/workflow/workflow_engine.py):
  _run_section(self, section: SectionDefinition, ctx: ExecutionContext) -> None
  _run_element(self, element, section, ctx, params_override: dict | None = None) -> None  # params_override added Wave 1
  self._collector  — ResultCollector; inspect self._collector.summary().steps for .element_name and status

ActionFactory (src/actions/action_factory.py):
  ActionFactory(section, wm, params=params)
  run(element)  — raises SkipElementSignal when skip_if_not_visible probe fails; raises ElementActionError on action failure

Exceptions (src/core/exceptions.py): SkipElementSignal, ElementActionError, WaitTimeoutError
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write TestIndexRange (model validation) + TestReservedParamName (loader) RED tests</name>
  <read_first>
    - tests/unit/test_workflow_models.py (modifying — copy TestElementDefinition pattern, lines 58-181, and imports lines 1-23)
    - tests/unit/test_json_loader.py (modifying — copy TestWorkflowLoader pattern, lines 21-148, and _write_json helper lines 22-25)
    - .planning/phases/22-support-updating-a-group-of-similar-web-elements-together-sa/22-PATTERNS.md (TestIndexRange and TestReservedParamName analog blocks)
  </read_first>
  <action>
    Append a `TestIndexRange` class to tests/unit/test_workflow_models.py covering the
    three Pydantic-validation behaviors. Method names MUST be exactly:
    `test_no_index_range_defaults_to_none` (D-01: field defaults to None when absent),
    `test_start_greater_than_end_raises` (D-02b: `index_range=[3, 0]` raises ValidationError,
    `match="start.*<=.*end|start.*must be"`),
    `test_length_not_2_raises` (D-02c: `index_range=[0, 1, 2]` raises ValidationError,
    `match="2-element"`). Also add `test_valid_index_range_accepted` ([0, 3] accepted) and
    `test_single_element_range_accepted` ([5, 5] accepted) as positive guards. Build elements
    via a local `_make_element(index_range=...)` helper using `ElementType.NUMBER`,
    `ActionType.INPUT`, `value="100"`, name `"amount_${index}"`, locator
    `LocatorDefinition(by="id", value="el_${index}")`. Use `pytest.raises(ValidationError)`
    exactly as the existing `test_required_input_without_value_raises` does.

    Append a `TestReservedParamName` class to tests/unit/test_json_loader.py covering the
    reserved-name behavior at BOTH loader entry points. Method names MUST be exactly:
    `test_index_param_raises` (workflow param named `index` -> `WorkflowValidationError` via
    `WorkflowLoader.load`, `match="reserved"`),
    `test_index_param_raises_in_load_raw` (same via `WorkflowLoader.load_raw`, `match="reserved"`),
    `test_non_reserved_param_accepted` (param `account_type` loads fine). Use a local
    `_write_json` helper and a `_workflow_with_param(name, value)` builder producing a minimal
    workflow dict (`workflow_name`, `start_url`, `parameters`, `tabs: []`), mirroring the
    PATTERNS.md analog. These tests implement D-01/D-02 validation and the reserved-name
    guard from RESEARCH.md.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_workflow_models.py::TestIndexRange tests/unit/test_json_loader.py::TestReservedParamName -x 2>&1 | grep -Eq "(failed|error)" && echo "RED-as-expected" || (echo "UNEXPECTED-GREEN: implementation may already exist" && exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/test_workflow_models.py` contains `class TestIndexRange` with the five named methods listed above.
    - `tests/unit/test_json_loader.py` contains `class TestReservedParamName` with `test_index_param_raises`, `test_index_param_raises_in_load_raw`, `test_non_reserved_param_accepted`.
    - Running these classes now produces failures/errors (RED) because `index_range` and the reserved check do not yet exist — this is the expected Wave 0 state.
    - No production source files (`src/`) are modified by this task.
  </acceptance_criteria>
  <done>TestIndexRange and TestReservedParamName classes exist with the exact method names from VALIDATION.md and fail because the implementation is absent.</done>
</task>

<task type="auto">
  <name>Task 2: Write TestIndexExpansion engine-level RED tests (no browser)</name>
  <read_first>
    - tests/unit/test_index_expansion.py (creating)
    - tests/unit/test_action_dispatch.py (analog — mock-page fixtures lines 36-52, side_effect sequencing lines 359-365, SkipElementSignal raise lines 377-389)
    - tests/unit/test_locator_resolver.py (analog — ActionFactory with params + patch.object capture, lines 98-170)
    - src/workflow/workflow_engine.py (target — _run_section lines 115-120, _run_element lines 122-176)
    - .planning/phases/22-support-updating-a-group-of-similar-web-elements-together-sa/22-PATTERNS.md (test_index_expansion.py structure block, lines 444-514)
  </read_first>
  <action>
    Create tests/unit/test_index_expansion.py with a `TestIndexExpansion` class that drives
    `WorkflowEngine._run_section` directly (NOT `engine.run()` — avoids navigator/page-load
    deps). Mock the browser surface: `MagicMock()` for driver and WaitManager; patch
    `src.actions.action_factory.ActionFactory.run` so `_run_element`'s real try/except block
    executes without Selenium. Use a real `ResultCollector` and assert on
    `engine._collector.summary().steps` (`.element_name` and status). Provide module-level
    `_make_locator(value="el_${index}")` and `_make_indexed_element(name, index_range, **kwargs)`
    helpers. Method names MUST be exactly:
    `test_range_0_to_3_produces_four_calls` (D-02a: range [0,3] -> ActionFactory.run called 4 times),
    `test_embedded_index_in_locator_value` (D-03: on the index=2 iteration the element passed to
    _run_element carries locator value resolving to `amount_2`; assert the per-iteration
    `merged_params["index"]` or captured concrete element reflects index 2 — capture via the
    patched run's call args),
    `test_xpath_locator_with_index` (D-03b: XPath `//input[@id='amount_${index}']` element loops
    and per-index params carry the index),
    `test_step_result_shows_concrete_name` (D-04: `summary().steps[2].element_name == "amount_2"`),
    `test_same_value_all_indices` (D-05: every captured concrete element has `value == "100"`),
    `test_n_results_for_n_indices` (D-07: range [0,3] -> `len(summary().steps) == 4`),
    `test_failed_index_does_not_stop_group` (D-08: `side_effect=[None, ElementActionError("x"), None, None]`
    -> 4 steps, exactly one FAILED, three PASSED, loop did not abort),
    `test_missing_index_skipped_when_skip_flag` (D-09: element built with
    `options={"skip_if_not_visible": True}`, `ActionFactory.run` side_effect raises
    `SkipElementSignal` -> that index status is SKIPPED not FAILED),
    `test_missing_index_failed_without_skip_flag` (D-09b: no skip flag, `ActionFactory.run`
    raises `ElementActionError` -> FAILED, remaining indices still run),
    `test_non_indexed_element_unchanged` (no-regression: an element with `index_range=None`
    produces exactly one step under its literal name and one `ActionFactory.run` call).
    Sequence multi-index outcomes with `side_effect` lists exactly as
    test_action_dispatch.py lines 359-365. Do not implement engine changes here.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_index_expansion.py::TestIndexExpansion -x 2>&1 | grep -Eq "(failed|error)" && echo "RED-as-expected" || (echo "UNEXPECTED-GREEN: implementation may already exist" && exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/test_index_expansion.py` exists with `class TestIndexExpansion` and the ten named methods above (matching VALIDATION.md command table).
    - Tests construct a real `WorkflowEngine` + real `ResultCollector` with `MagicMock()` driver/WaitManager and patch `src.actions.action_factory.ActionFactory.run`.
    - No `time.sleep()` and no real browser launch anywhere in the file.
    - Running the class now fails/errors (RED) because `index_range` and `params_override` do not yet exist.
    - No production source files (`src/`) are modified by this task.
  </acceptance_criteria>
  <done>tests/unit/test_index_expansion.py exists with all ten TestIndexExpansion methods named per VALIDATION.md, runs without a browser, and fails because the implementation is absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow JSON author -> framework | Untrusted authored `index_range`, `${index}` tokens, and `parameters` cross into the engine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering | `${index}` substitution into locator/name | mitigate | Tests assert index is integer-sourced; `str(i)` yields a safe decimal. `test_xpath_locator_with_index` proves no injection beyond a numeric literal. Real enforcement lands in Plan 02; this plan locks the test that would catch a regression. |
| T-22-02 | Denial of Service | unbounded `index_range` loop | accept (in this plan) | This plan only writes tests; the range-cap decision is exercised/owned in Plan 02's threat_model. No test here asserts a cap (Claude's Discretion deferred to Plan 02). |
| T-22-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase — RESEARCH.md "Package Legitimacy Audit: not applicable". No install tasks, no legitimacy checkpoint required. |
</threat_model>

<verification>
Run both new test classes plus the new file; ALL must be RED (failing/erroring) because the
implementation is intentionally absent:
`python -m pytest tests/unit/test_index_expansion.py tests/unit/test_workflow_models.py::TestIndexRange tests/unit/test_json_loader.py::TestReservedParamName -v`

The existing suite must be UNAFFECTED (new tests do not touch src/):
`python -m pytest tests/unit/test_action_dispatch.py -x`
</verification>

<success_criteria>
- All three test artifacts exist with the exact class and method names from VALIDATION.md.
- The full Wave 0 matrix (D-01..D-09, reserved, no-regression engine test) is represented by a named test.
- Tests are RED now and require zero edits to pass once Plan 02 implements the feature.
- Engine tests run with mocked driver/WaitManager and a real ResultCollector — no browser.
- No `src/` files modified.
</success_criteria>

<output>
Create `.planning/phases/22-support-updating-a-group-of-similar-web-elements-together-sa/22-01-SUMMARY.md` when done
</output>
